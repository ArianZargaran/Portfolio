import React from "react";

import Modal, { UseModalAttrs } from "~/hooks/useModal";

const RepleatModal: React.FC<UseModalAttrs> = (props) => (
  <Modal {...props}>Repleat</Modal>
);

export default RepleatModal;
