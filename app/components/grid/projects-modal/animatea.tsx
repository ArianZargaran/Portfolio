import React from "react";

import Modal, { UseModalAttrs } from "~/hooks/useModal";

const AnimateaModal: React.FC<UseModalAttrs> = (props) => (
  <Modal {...props}>Animatea</Modal>
);

export default AnimateaModal;
